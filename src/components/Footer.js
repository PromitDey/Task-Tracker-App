import React from "react";
import { AppBar, Toolbar, Typography } from "@mui/material";

const Footer = () => {
  return (
    <div>
      <AppBar position="static" className="text-center p-2 text-2xl font-semibold">
        Created By Promit Dey
      </AppBar>
    </div>
  );
};

export default Footer;
